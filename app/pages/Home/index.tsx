import HomeCarousel from "@/app/components/HomeCarousel";

const Home = () => {
  return (
    <div className="flex flex-col items-center justify-center content-center w-full bg-[#1c1c1c] text-white">
      <HomeCarousel />
    </div>
  );
};

export default Home;
